# 📄 第二期 PRD｜StickS3「程序员鼓励师」× deepseek-harness

> **文档版本**：v2.1（2026-08-20，按用户产品愿景重写：女声音效 / 二次元角色 / 手势审批为核心）
> **基线**：一期已交付（Web 版 dsh + 设备状态镜像 + A/B 物理审批，全部 live 验证；见 `dsh-hardware-buddy/probe-report.md`）
> **产品定位**：把 StickS3 做成工位上的**程序员鼓励师**——二次元美女形象 + 女声语音反馈 + 空中手势审批，替你看守 dsh 的危险操作。

---

## 1. 二期目标（用户定义的三个核心体验）

| # | 功能 | 一句话 |
|---|---|---|
| **A** | **女声音效（鼓励师人格）** | 同意时「好哒，通过~」，拒绝时「不行哦，再检查一遍嘛~」，报错时「加油哦，bug 是成长的营养~」|
| **B** | **二次元美女角色** | 屏幕角色从 ASCII 宠物换成二次元美女（或仿真动画），覆盖全部状态 |
| **C** | **空中手势审批** | 空中**画圆 = 同意**，**画 X = 拒绝**，A/B 物理键保留兜底 |

产品人格：**温柔鼓励师**——审批时轻声确认，报错时打气，不催不吵。

## 2. 用户场景

| 场景 | 触发 | 期望体验 |
|---|---|---|
| 危险命令待审 | dsh 调 bash/write | 美女角色 attention 动画 + 屏显工具 → 空中画个圆 → 「好哒，通过~」+ 撒花 |
| 想拒绝 | 同上 | 空中画 X → 「不行哦，再检查一遍嘛~」|
| 工具报错 | bash 非零退出/测试失败 | 角色皱眉握拳 + 「加油哦，bug 是成长的营养~」|
| 长任务跑批 | agent 长时间运行 | 角色忙碌动画，完成时提示音 + 偶发待机轻语（「记得喝水哦」）|
| 离开工位 | 拔线/进程退出 | 角色入睡，不吵人 |

## 3. 功能需求

### FR1（🔴）女声音效包

**语音矩阵（最小集）**：

| 场景 | 文案 | 语气 |
|---|---|---|
| approve（放行）| 好哒，通过~ | 轻快开心 |
| deny（拒绝）| 不行哦，再检查一遍嘛~ | 温柔坚定 |
| error（报错鼓励）| 加油哦，bug 是成长的营养~ | 元气 |
| idle 待机轻语（×2）| 记得喝水哦 / 你今天也很努力呢 | 轻声 |
| boot 开机 | 我来陪你写代码啦~ | 活泼 |

- 预生成 WAV（16kHz/16bit/单声道，每条 1~3 秒）烧入设备，**不做实时 TTS**
- 音量上限默认 75%（供电保护），设置菜单可调
- 触发替换现有蜂鸣：approve/deny 路径直接换语音；error 为新增触发（协议加 `error_seq` 字段，与完成音同模式）

### FR2（🔴）二次元美女角色包

- 新角色 `characters/encourager/`：沿用现有 bufo 角色系统（manifest.json + 每态 GIF，设备端零新代码）
- 状态覆盖：sleep / idle（多帧）/ busy（敲代码小动画）/ attention（眨眼招手）/ celebrate（撒花）/ dizzy（歪头）/ heart（比心）
- 设置菜单可切换角色（现有机制），ASCII 宠物全部保留
- 资产规格以 bufo 实测为基准（P2-0 摸底尺寸/帧数/体积），单态建议 ≤8 帧

### FR3（🔴）空中手势审批（画圆 = 同意，画 X = 拒绝）

| 手势 | 含义 | 动作 |
|---|---|---|
| ✋ 空中画圆 | 同意 | 发送 `permission once`（与 A 键同路径）|
| ❌ 空中画 X | 拒绝 | 发送 `permission deny`（与 B 键同路径）|

交互规则：
- **仅在审批悬挂时识别**（平时画圆不误触）
- **平放桌面不识别**（防震动误判）；识别中角色切 dizzy 给视觉反馈
- 单次识别后 **1.5 秒锁定**防重复
- 识别成功 → 对应动画 + 对应语音
- A/B 物理键永远保留（手势是增强不是替换）

### B 线（🟡 顺手小件，与主题协同）

| # | 内容 | 说明 |
|---|---|---|
| FR4 | 完成提示音 | turn 结束设备轻响一声（插件发递增 `completion_seq`，固件已支持，约 6 行代码）|
| FR5 | 设备时间同步 | 心跳携带 `time:[epoch,tz]`（协议已支持），不配 WiFi 时钟也准 |
| FR6 | 工程卫生 | 多实例串口锁（双开不抢设备）+ 品牌清理（Codex→DSH 残留字样）|

### 三期候选（本轮不做）

配额显示（数据源调研）、token 跨日持久化、Web UI 设备徽标、多设备消歧、冷启动竖屏回退、HIL、Windows、正式 OTA 信任引导。

## 4. 非功能需求

|ID|需求|
|---|---|
|NFR1|手势延迟：动作完成到回包 < 300ms；语音播放无破音、起播 < 500ms|
|NFR2|存储预算：角色包 + 语音 ≤ 2MB（8MB Flash 内）；不挤占 OTA 分区|
|NFR3|功耗：电池场景连续审批 ≥ 4h（音量上限 + idle 降频）|
|NFR4|误触安全：平放 5 分钟零误触发；无悬挂审批时手势完全静默|
|NFR5|回退完备：语音/角色/手势任一失效时，A/B 键 + 蜂鸣 + ASCII 宠物仍完整可用|

## 5. 验收标准

|#|用例|通过条件|
|---|---|---|
|AC-P2-1|语音触发|三句核心语音（同意/拒绝/报错）各场景实播，清晰无破音；音量受限可调|
|AC-P2-2|报错联动|真实 dsh 任务中工具失败 → 设备播报错语音 + 对应动画|
|AC-P2-3|角色包|七态 GIF 全部加载播放；设置菜单切换成功；ASCII 宠物可切回|
|AC-P2-4|手势识别率|坐姿握持，画圆/画X 各 20 次，识别率 ≥ 85%|
|AC-P2-5|手势防误触|平放桌面 5 分钟零触发；无悬挂审批时随意晃动零反应|
|AC-P2-6|手势闭环|画圆 → dsh 放行继续；画 X → dsh 拒绝（与 A/B 键等价，串口回包一致）|
|AC-P2-7|B 线小件|完成提示音响起；时钟正确；双实例不抢设备|
|AC-P2-8|回归|A/B 键审批、状态镜像、拔插回落等一期能力全部回归通过|

## 6. 里程碑（约 8.5 天 + 1 buffer）

|Phase|内容|工期|说明|
|---|---|---|---|
|P2-0|可行性摸底：WAV 播放管线验证（M5.Speaker.playRAW）、bufo 角色打包机制与资产规格、IMU 陀螺仪可用性|0.5d|本地，产出规格基线|
|P2-1|语音包：固件 WAV 播放管线 + 5 条语音接入 + 音量约束 + `error_seq` 协议小扩展|2d|素材生成可并行（AI 配音工具）|
|P2-2|角色包：manifest + 七态 GIF 资产接入 + 切换验证|2d|**素材是长杆**，可远程生成/用户自备|
|P2-3|手势识别：采集窗口 + 圆/X 判定 + 防误触门控 + 对接审批路径 + 调参|2.5d|风险最高，识别率迭代|
|P2-4|联调打磨 + B 线小件（FR4/5/6）+ 全量回归|1.5d|—|

## 7. 非目标

- 不做实时 TTS / 本地 LLM（设备算力不允许，预生成语音）
- 不做 BLE/无线恢复（继续 USB CDC）
- 不修改 deepseek-harness 源码
- 不做多角色语音包系统（单人格，先跑通）

## 8. 风险

|风险|影响|对策|
|---|---|---|
|手势识别率不达标（≥85%）|核心体验受损|分参数迭代（窗口/闭合度/半径带）；最坏情况降级为"手势 beta、按键为主"|
|二次元 GIF 素材质量/体积超标|延期或降画质|以 bufo 体积为预算基线；AI 生成 + 压缩管线；先用简笔风格占位跑通|
|语音播放与渲染争资源|卡屏/爆音|播放走独立通道（M5.Speaker 异步）；限制并发播放|
|戴手套/走路误触|误审批|产品边界写明"坐姿握持使用"；平放抑制 + 锁定兜底|
